import type { ColorSchema } from './theme.types';

/* The sample tenant the ColorSchema dialog starts from. Deliberately nothing like the brand teal. */
export const DEFAULT_TENANT_COLORS: ColorSchema = {
    primary: 'rgb(114, 46, 209)',
    primaryFont: 'rgb(38, 27, 60)',
    primaryBackground: 'rgb(255, 255, 255)',
    secondaryFont: 'rgb(124, 106, 152)',
    secondaryBackground: 'rgb(246, 243, 251)',
};
